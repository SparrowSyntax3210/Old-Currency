# Customer Frontend

Static customer-facing pages for browsing products and account flows.

Run a local static server from this folder:

```powershell
python -m http.server 5500
```

Available pages include `product.html`, `product-information.html`, and `auth/auth.html`.
