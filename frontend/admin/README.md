# Admin Frontend

React, TypeScript, Tailwind CSS, and Vite dashboard for marketplace administrators and sellers.

```powershell
npm install
npm run dev
```

Build for production with `npm run build`.
